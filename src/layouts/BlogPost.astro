---
// ... (import dan type Props tetap sama) ...
import type { CollectionEntry } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';


type Props = CollectionEntry<'blog'>['data'] & { customSlug?: string; originalId?: string; }; //Tambahkan customSlug dan originalId


const { title, description, pubDate, updatedDate, heroImage, customSlug, originalId} = Astro.props; //Ambil custom slug
---

{/* ... (HTML tetap sama) ... */}
<html lang="en">
<head>
    <BaseHead title={title} description={description} />
    {/* ...style */}
</head>

<body>
    <Header />
    <main>
        <article>
            {/* ... */}
            <div class="prose">
               {/* .... */}
                <slot />
            </div>
        </article>
    </main>
    <Footer />
</body>
</html>
